<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home_index') }}">Evaluation</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mx-auto">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_index') }}">Users</a>
                </li>
            {% endif %}

        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                {% if app.user %}
                    <a class="btn btn-outline-success" href="{{ path('app_logout') }}">deconecte de {{ app.user.email }}</a>

                {% else %}
                    <a class="btn btn-outline-success" href="{{ path('app_login') }}">Login</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>